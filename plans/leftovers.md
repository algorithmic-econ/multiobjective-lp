## Leftovers

Use this file as a memory between sessions that implement independent tasks
---

### From T01 (PR #37, 2026-07-12)

Env / machine state (NOT in repo):
- experiments venv = py3.12 (`poetry env use /opt/homebrew/bin/python3.12`). T04 bumps to 3.13.
- PyPI `muoblpbindings` stale (0.0.16, lacks `expanding_approvals`/`stv`/`scr`) → `import muoblpsolvers` fails from clean install. Workaround baked into current venv: local build of sibling repo `../muoblpbindings` (0.0.17) via `pip install --ignore-requires-python` (repo requires py>=3.13). Any fresh venv before T04/T06 needs same step.
- THIS MACHINE: CommandLineTools broken — toolchain missing `c++/v1` headers, bare `clang++` can't compile hello-world. Bindings build needs `SDKROOT=$(xcrun --show-sdk-path) CXXFLAGS="-cxx-isystem $SDKROOT/usr/include/c++/v1"`. Proper fix: reinstall CLT (`xcode-select --install`, needs user). Affects T06/T07 local verify.

Discovered bugs fixed in T01 beyond plan (context for T02 golden):
- B6: non-dedup objective weights must be int (`1` not `1.0`) — MES bindings take `long long` utilities, pybind rejects float. Dedup path uses `len()` (int) already. If anyone reintroduces float weights/utilities, every MES solve dies with pybind TypeError.
- B7: runner must call `problem.write_lp` (custom, appends OBJECTIVES/WEIGHTS) not pulp `writeLP` — analyzer `read_lp_file` requires those sections. Contract covered by `tests/test_lp_roundtrip.py`.

Deferred (has ticket):
- Analyzer `except → None` swallows real errors (made B7 diagnosis painful) → T24.
- `config/logging_config.yaml` FileNotFoundError noise when scripts run from `sample-experiment/` (relative path from cwd); non-fatal → T20/T09.
- `analysis_table` KeyError `Location-Year` when ALL rows fail regex (empty df) — only partial fix done (ValueError w/ pattern) → T24.
- Stale tracked generated file `sample-experiment/results/sample-analysis/metrics-sample-experiment.jsonc` (old ext; new pipeline writes `.json`, which is untracked) → prune in T09.
- MES_CONSTRAINT/MES_EXPONENTIAL/PHRAGMEN with empty `solver_options` still crash inside `actualSolve` (missing `max_iterations` etc.) → T10/T13.

Facts useful for T02 (golden e2e):
- Sample run: 4 configs (all MES_ADD1), full krakow_2024 = 19 districts, ~2 min wall on this machine; analyze ~1 min.
- Reference metrics from T01 verify run (exclusion_ratio / sum_obj / ejr+): APPROVAL 0.0033/219239/167; COST 0.0035/134763199377/0; COST_ORDINAL 0.0032/279444152847/0; bronowice COST_ORDINAL 0.0666/4351588280/0.
- Meta json `time` field + filename timestamp+uuid are nondeterministic → normalization list (D11).
- pulp preserves int coefficients in `LpAffineExpression` (verified) — int utilities survive to bindings.

Repo conventions observed:
- pre-commit hooks active (ruff, ruff-format, eof, trailing-ws) — commits auto-checked.
- `poetry -C <dir> run` changes cwd → breaks relative-path scripts; `cd` first instead.
- Branch naming used: `feat/t01-fix-pipeline` (existed already; plan's `fix/...` name skipped).

### From T02 (PR #38, 2026-07-12)

- e2e golden: `cd experiments && poetry run pytest -m e2e`; regen `UPDATE_GOLDEN=1 ...` (writes goldens then FAILS by design — inspect + commit separately). Normalization list (D11) in `tests/golden_utils.py`, awaiting confirm in PR #38 review.
- Goldens macOS-generated; C++ MES tie-breaks may differ on Linux CI → surfaces in T03; `sorted(os.listdir)` fix removed main known cause.
- ~~DISCOVERED: GreedySolver `total_utility[candidate]` KeyError on zero-vote candidates (`greedy_solver.py:43`; full krakow never hits it). Fixture avoids via coverage-first ballot pick. Fix in T13 (validation) or T17 (tests).~~ **FIXED in T17** — `.get(candidate, 0)` in both sort-key + filter; regression test `test_greedy_ignores_zero_vote_candidate`.
- DISCOVERED: meta `instance_size`=11 not 10 — pulp `__dummy` passes `problemRunner.py` filter `name != "dummy"` (actual name `__dummy`). Metric INSTANCE_SIZE correct (10). Golden captures 11; fix in T19 (meta remodel) → goldens regen there.
- MES_ADD1 golden has `invalid_count: 1` — Add1 exceeds district cap by design, not a bug.
- `pytest.register_assert_rewrite("tests.golden_utils")` needed in conftest for readable golden diffs (+ E402 per-file-ignore); pattern applies to any future assert-helper module.
- tests/ is a package (relative imports, `tests.golden_utils` module path).

### From T03 (PR #39, 2026-07-12)

Env / machine state (NOT in repo):
- experiments now py3.13; venv `muoblpexp-*-py3.13` (poetry cache, not in-project locally). Lock regenerated → `muoblpbindings` 0.0.17 resolved from **PyPI** (macos-arm64 wheel). T01 local-build-of-sibling-repo hack now OBSOLETE for experiments. Transitive bumps accepted silently (notebook 7.6.0, coverage 7.15.0, etc.).
- core + solvers venvs also recreated on 3.13 (`poetry env use /opt/homebrew/bin/python3.13`). core pyproject still `requires-python >=3.11` (T04 bumps to >=3.13); CI runs 3.13 which satisfies it.
- core got a **dev group** (pytest, pyright) — previously none; core had no tests dir (pytest exit-5). Added `core/tests/test_import.py` smoke test.

pyright suppression inventory (D12 ratchet backlog — all basic mode):
- **core**: 1 inline `# pyright: ignore[reportInvalidTypeForm]` in `multi_objective_lp.py:16` (`sense: LpMaximize|LpMinimize` — pulp consts are ints not types; real fix T27).
- **solvers**: config `reportArgumentType: none` (15 systemic errors — `Utility`/int + float/int + dict-value invariance across mes_add1, mes_constrains, mes_exponential, mes_utility, phragmen). Plus 2 inline: `mes_constrains.py:21` reportOptionalOperand (Optional `constraint.value()`; None-guard T13), `phragmen.py:398` reportReturnType (returns list, annotated set[str]; T16/T27).
- **experiments**: config disables 10 rules (reportArgumentType 31, reportCallIssue 7, reportAttributeAccessIssue 7, reportAssignmentType 7, reportOperatorIssue 6, reportOptionalSubscript 5, reportReturnType 4, reportGeneralTypeIssues 3, reportTypedDictNotRequiredAccess 3, reportInvalidTypeForm 3 = 76 total). Loose pabutools/pandas interop + tests; most files rewritten in Phase 3 (T19 Pydantic, T21 rename, T22/T23 consolidate, T25 dead-code) which should eliminate most — re-enable rules incrementally as Phase 3 lands.

CI (test.yml) notes for T07/future:
- Rewrote: 3-project matrix (core/solvers/experiments) × {test, pyright} jobs, py3.13, `fail-fast: false`. Cache path per-project `${{ matrix.project }}/.venv`, key hashes all 3 locks (path-dep interdependence). No path filters; push trigger limited to `main` + `feat/roadmap-base-branch`; PRs always run.
- **UNVERIFIED locally: Linux CI e2e golden** — goldens macOS-generated; MES C++ tie-break divergence risk (leftovers T02). If ubuntu e2e diffs: normalization fix (`experiments/tests/golden_utils.py`) or switch experiments matrix to `macos-15` + GH issue. NO golden regen.
- `poetry install` runs unconditionally (no cache-hit guard) so editable path-deps (core←solvers←experiments) re-link against checkout.

### From T04 (PR #40, 2026-07-12)

- Trivial ticket — T03 had done most. Only 3 source edits: `core/pyproject.toml` requires-python `>=3.11`→`>=3.13`, `publish.yml:29` setup-python `3.12`→`3.13`, relock ×3.
- Relock diffs MINIMAL, zero transitive bumps: core lock `[metadata] python-versions`→`>=3.13` + content-hash; solvers/experiments only the `muoblp` path-dep `python-versions`→`>=3.13`. (No `poetry install` changes — all venvs already 3.13 from T03.)
- All green locally: pytest core 1 / solvers 13 / experiments 75 (incl e2e golden, no regen), ruff clean, pyright 0 errors ×3. pyright suppression inventory (T03 D12 backlog) unchanged.
- publish.yml change NOT CI-verifiable (tag-triggered) — review-only, rc-tag dry run deferred to T08.

### From T05 (PR feat/t05-pulp-332, 2026-07-12)

- pulp 2.9.0 → 3.3.2 across core+solvers pins; experiments relock-only (transitive). Relock ×3 minimal: only pulp 2.9.0→3.3.2, zero transitive bumps. All green: pytest core 1 / solvers 13 / experiments 75 (incl e2e golden + roundtrip, NO regen), ruff clean, pyright 0×3.
- 2 source fixes: `lp_reader_utils.py` `LpConstraint.from_dict({...constant:-c_rhs, pi})` → `LpConstraint(LpAffineExpression(c_lhs), sense=, name=, rhs=c_rhs)` (from_dict REMOVED in 3.x); `multi_objective_lp.py` +`pulp.set_v4_migration_warnings(False)` after LpCplexLPLineSize monkeypatch (silences all 3.3.x v4 DeprecationWarnings centrally; -W error::DeprecationWarning clean ×3).
- Solve results IDENTICAL post-bump: sample metrics match T01 refs exactly (APPROVAL 0.0033/219239/167; COST 0.0035/1.34763e11; COST_ORDINAL 0.0032/2.79444e11; bronowice 0.0666/4.35159e9). e2e golden unchanged.
- NEW pyright suppression debt (pulp 3.3.2 stubs typed `LpElement.name` + `LpConstraint.value()` as Optional; was green after T04, so all bump-induced) — 6 inline `# pyright: ignore`, all tagged w/ pulp-3.3.2 reason:
  - solvers `mes_constrains.py:34,35` reportOptionalOperand (value()<0/>0; None-guard T13); `election_solver.py:113` reportCallIssue (.get(name)); `common.py:58,59` reportAssignmentType+reportCallIssue (dict[name] comp).
  - experiments `tests/test_constraint_creation.py:276,277` reportOptionalMemberAccess (.name.startswith).
  - Root cause uniform: `name` Optional str → fixable en masse when name-typing addressed (T27-ish). No config-rule changes.
- **pulp-4.0 migration debt** (out of roadmap scope; kill switch masks now): `PULP_CBC_CMD` removal (COIN_CMD switch), direct `LpVariable(...)` ctor + `LpVariable.dicts` + `addVariable(s)`, dict-like `prob.constraints` (→ list API). All heavily used (election_solver, pabutoolsToMoLp, lp_reader_utils, mes/common, phragmen). File GH issue when pulp 4.x targeted.
- Known non-fatal noise unchanged: `config/logging_config.yaml` FileNotFoundError when run from sample-experiment/ cwd (T20/T09); pulp `UserWarning: Spaces not permitted in name` during sample solve (pre-existing, not a Deprecation).
- CI: pushed branch → PR to feat/roadmap-base-branch; Actions unverified locally (Linux e2e tie-break risk per T02/T03 leftovers still applies).

### From T06 (PR feat/t06-merge-bindings, 2026-07-12)

- Squash-imported `../muoblpbindings@89f6b23` (chore/publish-release, v0.0.17) → `bindings/` (4th subproject) via `git archive | tar -x`; dropped stale `poetry.lock`. 17 files. History stays in old repo (user archives GH manually post-merge — NOT done).
- solvers/experiments pin `muoblpbindings="0.0.17"` (PyPI) → path dep `{path="../bindings", develop=false}`; relock ×2 (wheel-files entry → `[package.source] type=directory url=../bindings`, zero transitive change). experiments resolves `../bindings` nested via solvers path dep (like `../core`).
- Fresh solvers venv (`env remove --all; env use python3.13; install`) builds bindings from source via scikit-build-core pip isolation. **macOS broken-CLT workaround REQUIRED** (broken-clt-cpp-headers memory): `export SDKROOT=$(xcrun --show-sdk-path); export CXXFLAGS="-cxx-isystem $SDKROOT/usr/include/c++/v1"`. No SDKROOT → build fails (missing c++/v1). Applies to any bindings-building install (solvers, experiments) on this machine + T07 local verify.
- Pre-commit hooks touched imported files (repo-wide, plan-anticipated): eof-fixer trimmed `.clang-format` double-newline; ruff F401 on shim `__init__.py` (re-export names) → added `__all__`; ruff-format reformatted `__init__.pyi` (wrapped multiline sigs). Used solvers-venv ruff 0.15.1 (pins match pre-commit; global ruff is 0.14.0 — mismatch, avoid).
- All green: pytest solvers 13 / experiments 75 (incl e2e golden + roundtrip, NO regen), ruff check+format repo-wide clean (80 files), pyright solvers 0 / experiments 0 (suppression inventory unchanged). `import muoblpbindings` OK, 5 fns exposed.
- **T08 data — path deps NOT vendored, emitted as broken `file://` URLs** (verified `poetry build` solvers on this branch): wheel bundles ONLY `muoblpsolvers/` (no bindings/core code inside). METADATA `Requires-Dist` = `muoblp @ file:///Users/jasiek/.../core` + `muoblpbindings @ file:///Users/jasiek/.../bindings` (both path deps → local file: direct refs, NOT version pins). Wheel UNPUBLISHABLE as-is (PyPI rejects file: direct refs; path absent on user machine). Poetry 2.x does NOT auto-rewrite path dep → version pin (roadmap T08 assumes it should — it currently does not). Fix in T08: force version-pin rewrite for muoblp + muoblpbindings; T18 sets bindings published range `>=0.0.17,<0.1`. Publish order: bindings (PyPI, via T07 wheels) FIRST, then core, then solvers depends by version. Confirms 3 independent PyPI packages — bindings is standalone, path dep is dev-only wiring.
  - Also in that METADATA: `pytest (==8.4.1)` as runtime `Requires-Dist` (should be dev group) → T18.
- Deferred (record for later tickets):
  - bindings `.pyi` imports `pulp` (`from pulp import LpProblem`) but bindings has no declared runtime dep → T18 candidate (add pulp range to bindings metadata, or drop import).
  - CI cache key hashes 3 locks; bindings C++-only edits don't bust it (no lock in bindings) → T07.
  - `bindings/.github/workflows/wheels.yml` inert (GH runs only root `.github`) → T07 ports to root w/ bindings paths + `bindings@x.y.z` tag trigger.
  - root README publish section still lists only core/solvers packageNames; bindings publish deferred to T07 (documented tag convention in bindings/README only).

### From T07 (PR feat/t07-bindings-ci, 2026-07-12)

- `git mv bindings/.github/workflows/wheels.yml .github/workflows/wheels.yml` + adapt; `bindings/.github/` removed. New root `wheels.yml`: triggers `workflow_dispatch` + `push tags bindings@X.Y.Z` + TEMP `push branches feat/t07-*` (dry-run) — **STRIP temp branch trigger (wheels.yml:5-7) before merge**. Jobs: `validate_tag` (regex + tag==pyproject version, tag-ref only), `build_sdist` (pipx build --sdist + twine check), `build_wheels` (ubuntu/macos-15/windows × cp313+cp314, cibuildwheel 3.4.1 `package-dir: bindings`, setup-uv, `git diff --exit-code`, auditwheel linux, ARM64 windows, MACOSX_DEPLOYMENT_TARGET=14.0), `upload_all` (gate `refs/tags/bindings@`, env pypi, OIDC). Dropped `submodules:true` (none). workflow_dispatch = build-only (upload skipped).
- test.yml: NEW `build-bindings` job (ubuntu py3.13, `python -m build --wheel bindings` → install → `import muoblpbindings` smoke → upload artifact `bindings-wheel`). `test` job `needs: build-bindings`; solvers-only steps download artifact + `poetry run pip install --force-reinstall --no-deps ../bindings-wheel/*.whl` (tests run vs built wheel). experiments/core stay path-dep source build. `pyright` job NOT gated on artifact. Cache keys (both jobs) extended: `hashFiles(...,'bindings/pyproject.toml','bindings/CMakeLists.txt','bindings/src/**')` — fixes T06 leftover (C++ edits now bust solvers/experiments caches).
- Local sanity (CLT workaround): `pipx run build --wheel bindings` OK (cp313 macos arm64); fresh py3.13 venv install + `import muoblpbindings` OK. NOTE `muoblpbindings.__doc__` is `None` → CI smoke prints "None" (non-failing, import success is the check). `bindings/dist/*.whl` gitignored (T06).
- System `python3` on this machine = **3.14** — cp313 wheel won't install under it; must use `/opt/homebrew/bin/python3.13` for local wheel smoke.
- READMEs updated (root + bindings/README) publish sections; dropped "inert" note.
- **UNVERIFIED**: no tag pushed → tag-triggered PyPI upload path (validate_tag + upload_all) never exercised; first real `bindings@0.0.18` (or T08 rehearsal) verifies. Full 6-wheel matrix dry-run via temp branch trigger requires push→Actions (not run locally). Windows/macos wheel quirks + cp314 → D14 note.
- Publish order reminder (from T06): bindings PyPI FIRST (this workflow), then core, then solvers by version — relevant to T08.

### From T08 (PR feat/t08-fix-publish, 2026-07-12)

- **Root fix — solvers wheel path-dep → version-pin via Poetry 2.0 dependency enrichment** (T06 `file://` bug): moved version pins into static `[project.dependencies]` (`muoblp>=1.0.4,<2.0`, `pulp==3.3.2`, `muoblpbindings>=0.0.17,<0.1`, `pytest==8.4.1`), dropped `dynamic=["dependencies"]`; `[tool.poetry.dependencies]` reduced to path-only enrich (`muoblp`→`../core` develop, `muoblpbindings`→`../bindings`). Poetry 2.x rule: a dep in BOTH tables uses `[project]` version for wheel METADATA + `[tool.poetry]` path for dev install/lock. Verified METADATA now `muoblp (>=1.0.4,<2.0)` / `muoblpbindings (>=0.0.17,<0.1)`, ZERO `file://`. `poetry lock` REQUIRED after (check fails otherwise); solvers lock changed, experiments lock UNAFFECTED (path-dep hash stable).
- pytest==8.4.1 STILL a runtime `Requires-Dist` (in-scope-boundary: T18 moves to dev group). Version-pinned so satisfies T08 AC; just misplaced.
- Bindings range `>=0.0.17,<0.1` set here (T18 was nominal owner per T06 leftover — now DONE); confirm/adjust in T18 if range policy changes. core range `>=1.0.4,<2.0`.
- **publish.yml changes**: (1) `validate-tag` now checks out + verifies tag version == `<pkg>/pyproject.toml` version (strips `-rc`; mirrors wheels.yml T07). (2) Dropped `poetry install` before `poetry build` (build reads pyproject only — no bindings C++ compile, no core install needed; verified solvers wheel builds w/o SDKROOT). (3) Added `twine check <pkg>/dist/*` metadata gate post-build. (4) `skip-existing: true` on pypa/gh-action-pypi-publish (idempotent re-push, avoids the version-already-exists 400 — chosen over hard-fail). (5) Header comment: `bindings@` tags route to wheels.yml (publish.yml tag filter already core/solvers-only, no code change needed).
- **wheels.yml**: STRIPPED stale `feat/t07-*` temp branch trigger (T07 pre-merge cleanup that was missed — it shipped live on base via PR #43). Now `workflow_dispatch` + `bindings@X.Y.Z` tags only.
- **ROADMAP reconcile**: T07 was merged (PR #43, commit ae5a2a9) but checkbox never flipped — set `[x]` + PR link this session.
- **UNVERIFIED locally** (per T04/T07 pattern — tag-triggered, needs OIDC/test.pypi): actual `solvers@X.Y.Z-rc` / `core@X.Y.Z-rc` push → test.pypi upload + `pip download` METADATA resolution. Review-only. First real rc tag (T08 rehearsal) exercises validate-tag + publish + skip-existing. NOTE: `pip download` of solvers wheel will only resolve if `muoblp`/`muoblpbindings` already on the target index (publish order bindings→core→solvers still holds).
- Local verify GREEN: core 1 / solvers 13 / experiments 75 (incl e2e golden, NO regen), ruff clean ×3, pyright 0 ×3. Wheel metadata: core (pulp==3.3.2, no file://) + solvers (all version-pinned, no file://) `twine check` PASSED. All 3 workflows `yaml.safe_load` OK. validate-tag bash sim: correct MATCH/MISMATCH. System python3 lacks pyyaml — used experiments venv for YAML parse; system python3 = 3.14 (T07 note) so wheel smoke needs 3.13 venv.

### From T09 (PR #45, 2026-07-12)

- Deleted (git rm, NOT archived — user decision, all unreferenced by src/tests): whole `experiments/resources/` (7 abs-path experiment configs incl. invalid `"MES"` solver_type + `no-constraints.json` ext bug — deletion moots both "fix stale configs" bullets; 6 analyzer configs; 2 constraint configs incl. only LB-strategy example `sample-constraints.jsonc` — schema recoverable from `model.py` ConstraintConfig + git history; `warszawa_2023_test/` 5.9MB .pb ×2; 6 tracked generated `analyzer-results/*.json`; 3 .gitkeep) + stale `sample-experiment/results/sample-analysis/metrics-sample-experiment.jsonc` (T01 leftover). ~27 files / ~7MB.
- ROADMAP "~25k file drop" was STALE: root `resources/` (26k files, 4.5G) untracked+ignored since pre-roadmap (last tracked b7ac995). Still on disk locally — user data, NOT touched.
- `.gitignore` consolidated: `/experiments/resources/` (whole dir) replaces 3 piecemeal rules (`**/*.png`, `experiment-results`, stale `/experiments/resources/results/`). Dir stays runtime-writable target for legacy scripts.
- T22/T23 heads-up: generator/aggregator scripts still default-output into deleted `resources/…` paths (`generatePhargmenGreedyDistrictExperiment.py:95` writes `../resources/input/experiment-config/…` — parent dir gone → runtime error until rewrite; `aggregateResults.py:238` `../resources/*.png`). Dir is gitignored so runtime writes harmless.
- logger fix done HERE not T20: `helpers/utils/logger.py` default config path now file-relative (`parents[3]/config/logging_config.yaml`) — kills FileNotFoundError noise when run from `sample-experiment/` (T01 leftover). All callers used default.
- Verify GREEN: experiments 75 pytest (incl e2e golden 1, NO regen), ruff+format clean, pyright 0. AC grep `/Users/jasiek` in *.json* empty. Sample run.sh+analyze.sh pass. core/solvers/bindings untouched (deletion-only ticket) — not re-verified.

### P1 judge verdict (2026-07-12, session post-T09)

- **P1 VERIFIED COMPLETE @047fadb — GO for P2.** Independent green-check: pytest core 1 / solvers 13 / experiments 75 (incl e2e golden), ruff repo-wide clean, pyright 0 ×3, `import muoblpbindings` OK, CI green on base.
- ROADMAP T05/T08 PR links fixed this session (#41, #44).
- Outstanding (non-blocking):
  - **T08/T07 publish paths never exercised end-to-end**: no rc tag pushed (test.pypi via publish.yml) nor `bindings@` tag (wheels.yml upload). Rehearse before first real release; order bindings→core→solvers.
  - Old `algorithmic-econ/muoblpbindings` repo NOT archived on GH (T06 manual follow-up; `isArchived:false`).
  - wheels.yml `workflow_dispatch` dry run triggered this session (closes T07 AC letter) — check outcome if not observed.
  - Cosmetic: solvers `[project.urls]` → `jasieksz/multiobjective-lp`, repo lives at `algorithmic-econ/` → T28.
- D8 (timeLimit on binding-backed solvers) still undecided — must be resolved before T14; T10–T13 unblocked.

### From T10 (PR feat/t10-native-options, 2026-07-13)

- Contract now: options = constructor kwargs → pulp `optionsDict` → serialized via `toDict()`. Solvers read `self.optionsDict[...]`, `self.solver_options`/`self.use_gurobi` GONE. No-option solvers (Greedy, Add1, Utility, STV, SCR, ExpandingApprovals) + ElectionSolver have NO `__init__` — inherit pulp base `(mip=True, msg=True, options=None, timeLimit=None, *args, **kwargs)`.
- Option defaults now in constructors (from generator spec): MES_CONSTRAINT cost_modification_base=1.007/max_iterations=200, PHRAGMEN increasing_scalings=False/kappa=1.0/bos_version=False/eps=1e-6, SUMMING use_gurobi=False. T01 leftover "empty solver_options crash" FIXED for these.
- MES_EXPONENTIAL `budget_init` REQUIRED at solve: None default dropped from optionsDict (pulp filters None kwargs) → `PulpSolverError` in actualSolve (was raw KeyError). T13 folds into shared validation. No numeric default invented (B_init tuning excluded §5).
- SolverOptions TypedDicts (mes_constrains, mes_exponential, phragmen) plain-deleted with migration (user-approved; superseded type decls, not archived).
- `get_solver` = dict dispatch + `**(solver_options or {})` unpack; config `solver_options` dict keys MUST be valid constructor kwargs. Config key `"use-gurobi"` renamed `"use_gurobi"` (generateExperimentConfig.py). T19 Pydantic SolverSpec must keep kwargs-compatible keys; config field name `solver_options` (RunnerConfig/meta/goldens) intentionally UNCHANGED until T19 — AC grep interpreted as solvers/-side clean (verified empty).
- pulp `optionsDict` gotcha: None-valued kwargs silently dropped; False kept. Anyone adding Optional options must read via `.get`.
- Sample smoke needs venv python on PATH: `run.sh`/`analyze.sh` call bare `python` → `PATH="$(poetry -C .. env info --path)/bin:$PATH" ./run.sh`. Also resultCache short-circuits solve — `rm -rf results/*` first for a real smoke (dirs gitignored). Metrics matched T01/T05 refs exactly.
- Verify GREEN: solvers 30 pytest (13→30, +17 contract tests), experiments 80 (75→80, incl e2e golden 1, NO regen), ruff+format clean ×2, pyright 0 ×2, sample e2e values identical. core/bindings untouched.

### From T11 (PR #47, 2026-07-13)

- `available()` on all 10 solvers now truthful (base `LpSolver.available()` raises NotImplementedError — 7 solvers previously inherited it, would've crashed if called). Binding-backed (STV, ExpandingApprovals, SCR, MES-Add1/Utility/Constrains) → `bindings_available()`; pure-python (Greedy, Phragmen via `ElectionSolver` base, Summed, MES-Exponential) → `True`. Removed 3 hardcoded `return True` stubs (STV/EA/SCR).
- NEW helper `muoblpsolvers/utils.py::bindings_available()` = `importlib.util.find_spec("muoblpbindings") is not None`. find_spec locates WITHOUT executing → no C++ init, no SDKROOT/CLT needed for the probe. Checks whole package (per ticket), not per-symbol.
- Lazy imports: 6 top-level `from muoblpbindings import X` moved to first line of each `actualSolve`. `import muoblpsolvers` now succeeds bindings-free (was hard-fail via `__init__` fan-out). Missing bindings now surfaces as ImportError at SOLVE time for binding-backed solvers (not import time) — T13 shared validation may wrap/pre-check via `available()`.
- Missing-bindings unit sim = `monkeypatch.setitem(sys.modules, "muoblpbindings", None)` (idiomatic): makes both `import muoblpbindings` raise AND `find_spec` return None. New `tests/test_available.py` (3 tests): import-without-bindings, available truthful without, available truthful with (skip-if-absent). solvers 30→33.
- Verify GREEN: solvers 33 pytest, experiments 80 (incl e2e golden 1, NO regen), ruff+format clean ×2, pyright 0 ×2 (no new suppressions). Bindings-free subprocess: `sys.modules['muoblpbindings']=None; import muoblpsolvers` → GreedySolver.available() True / STV.available() False. core/bindings untouched.

### From T12 (PR feat/t12-status-contract, 2026-07-13)

- Audit (all 10 solvers + pulp 3.3.2 source read): only 3 solvers actually violated the contract — `GreedySolver`, `MethodOfEqualSharesConstrainsSolver`, `MethodOfEqualSharesExponentialSolver` (matches ticket text exactly). Remaining 7 already compliant pre-ticket: Phragmen/ExpandingApprovals/SolidCoalitionRefinement/STV/MES-Add1/MES-Utility already call `utils.set_solved(lp, selected)` + `return lp.status`; `SummedObjectivesLpSolver` compliant via delegation — `PULP_CBC_CMD`/`GUROBI_CMD.actualSolve` call `lp.assignStatus(...)` internally (pulp `COIN_CMD.solve_CBC` line ~99, `GUROBI_CMD.actualSolve` line ~49).
- Fix pattern (all 3): collect/capture the `selected` list the solver already computes, then `set_solved(lp, selected); return lp.status` at the end of `actualSolve`/`_solve_election` — mirrors the pattern already used by Phragmen/MES-Add1/MES-Utility. No feasibility-based status branching added (T13 owns validation/rejection); MES-Constrains exhausting `max_iterations` while still infeasible still reports `LpStatusOptimal`, consistent with existing solvers' behavior.
- `set_solved` (already existed in `utils.py`, added T11-era) unchanged: `vals = {x.name: int(x.name in selected) for x in lp.variables()}; lp.assignStatus(LpStatusOptimal); lp.assignVarsVals(vals)`.
- AC "status asserts on ALL existing solver tests": only 3 test files actually call `problem.solve(solver)` for a real solve — `test_greedy.py` (3 tests), `test_mes_add1.py` (1), `test_mes_base.py` (1). `test_common.py`/`test_available.py`/`test_solver_contract.py` don't solve a real problem, untouched. Full T17 coverage sweep (Phragmen/MES-Constrains/MES-Exponential/Summed/EA/STV/SCR unit tests) still owed — this ticket only touched pre-existing solve-tests.
- Verify GREEN: solvers 33 pytest (count unchanged — only assertions added, no new tests), ruff+format clean, pyright 0 (no new suppressions). experiments 80 pytest incl e2e golden (NO regen — solver output values/selection unchanged, only status/return wiring). core/bindings untouched (solvers-only ticket).

### From T13 (PR feat/t13-raise-incompatible, 2026-07-13)

- **SCOPE NARROWED mid-session by user directive** (saved to memory `multiobjective-lp-pb-validation-scope`): PB-shape validation function `validate_election_program` lives in `election_solver.py`, called from `ElectionSolver.actualSolve`. Explicitly NOT added to `muoblpsolvers/utils.py` (generic, shared by all 10 incl. non-PB `SummedObjectivesLpSolver`).
- **FOLLOW-UP (same session, next user question): extended to the other 7 PB solvers.** Rules 1/3/4/5 (no objectives, missing/dup PB constraint, negative obj/constraint coefficient) and rule 2 (binary-var-domain) never touch `ElectionSolver`-specific state (the `Election` TypedDict / `molp_to_simple_election` transform) — they only read `lp.objectives`/`lp.constraints`, so `validate_election_program(lp)` is now also called as the first line of `actualSolve` in `mes/mes_add1.py`, `mes/mes_utility.py`, `mes/mes_constrains.py`, `mes/mes_exponential.py`, `single_transferable_vote.py`, `expanding_approvals.py`, `solid_coalition_refinement.py`. Confirmed safe against real data: every `Utility` strategy in `experiments/.../pabutoolsToMoLp.py::ballot_to_expression_strategy` (APPROVAL/COST/ORDINAL/CUMULATIVE/COST_ORDINAL/COST_CUMULATIVE) produces non-negative coefficients, and the pipeline always builds vars with `cat="Binary"`. Function name/location kept as-is (not renamed to something ElectionSolver-neutral, not moved out of `election_solver.py`) to minimize diff — flagged as optional cleanup if it ever reads oddly having 7 unrelated files import from `election_solver.py`.
- Still excluded: `SummedObjectivesLpSolver` (deliberately generic LP/MIP pass-through — the only real "generic, non-PB" case). Still open: GE/lower-bound-constraint rejection for MES/STV/EA/SCR (dropped rule, #36 "not for MES/STV") — these solvers still silently ignore GE constraints (wrong-answer risk, not just crash) rather than rejecting them. `mes/common.py::get_total_budget_constraint`'s own duplicate PB-constraint check is now mostly redundant at the `actualSolve` call sites (validation runs first and would already have raised) but still needed standalone since it's called directly by `test_common.py` and by `prepare_mes_parameters` outside any guaranteed-validated path — not collapsed, still T16's job.
- Test coverage: added `PB_SOLVER_CLASSES` list (mirrors `ALL_SOLVERS` in `test_solver_contract.py`, minus `SummedObjectivesLpSolver`) + one parametrized wiring test (`test_validation_wired_into_every_pb_solver`, "no objectives" rule only — the 5 rules' *logic* stays covered once via `GreedySolver`, this just proves each solver's `actualSolve` actually calls `validate_election_program` before doing solver-specific work). Skips per-solver on `not solver.available()`; none actually skipped in this venv (bindings present).
- Verify GREEN (this follow-up): solvers 42→51 pytest, ruff+format clean, pyright 0 (no new suppressions). experiments 80 pytest incl e2e golden (NO regen — MES_UTILS/MES_ADD1 in the e2e solver set now validated too, tiny fixture still well-formed).
- 5 rejection rules implemented in `validate_election_program`: (1) no objectives, (2) non-0/1-binary variable — `cat != "Integer"` (pulp normalizes `cat="Binary"` → `cat="Integer"` + `lowBound=0`/`upBound=1` at `LpVariable.__init__` time; checking literal `"Binary"` would reject every legitimately-binary var — verified via `inspect.getsource`), (3) missing/duplicate PB constraint, (4) negative objective coefficient, (5) negative PB-constraint coefficient. All raise `PulpSolverError` naming the offending var/constraint.
- Dropped rule (was in original draft, cut with the scope narrowing): rejecting GE/lower-bound constraints for solvers that can't handle them (#36: "makes sense for Greedy/PAV, not MES/STV"). `pb_with_lb_factory` + `test_greedy_solver_lb_*` (existing, GE constraint forces a low-ratio candidate) untouched — Greedy still allows GE via `is_feasible`. MES-family silently ignoring GE constraints (wrong-answer risk, not just crash) is now explicitly a known gap, not caught by anything — same follow-up ticket as above should pick this up too.
- Mechanical `Exception`/`ValueError` → `PulpSolverError` swap (no new checks) in `election_solver.py::validate_pb_constraint` and `mes/common.py::get_total_budget_constraint` — both existing PB-constraint-count checks now raise the same exception type ticket AC calls for. `test_common.py`'s `pytest.raises(Exception)` assertions still pass (`PulpSolverError` is an `Exception` subclass) — message text unchanged, not touched.
- Bonus: closed all 3 `mes_constrains.py` pyright suppressions tagged "T13" in the T03/T05 leftover inventory (`reportOptionalOperand` on `constraint.value()`, lines 21/34/35). **Revised mid-review**: first cut added a `_require_value()` helper that raised `PulpSolverError` on `None` — but that condition is unreachable by construction (every variable gets `setInitialValue` before any constraint is read, every loop iteration in `actualSolve`), so a runtime raise there is error-handling for a scenario that can't happen. Replaced with `typing.cast(float, constraint.value())` + one comment explaining the invariant — same pyright outcome (0 errors, no suppression), no dead exception path. Lesson: a pyright-Optional on a value doesn't always mean "validate input" — check whether the invariant is already structurally guaranteed before reaching for a raise.
- `validate_election_program` calls `validate_pb_constraint`, and `molp_to_simple_election` (called right after in `actualSolve`) calls it again — constraint list is walked twice per solve. Cheap at current fixture sizes; T16 (owns full transform/feasibility dedup) is the natural place to collapse this if it ever matters.
- New `solvers/tests/test_validation.py` (9 tests, one per rule + 3 variable-domain sub-cases + 1 "valid program still solves" sanity check using `PhragmenSolver`) — mutates `basic_pb_approval`/`invalid_pb` fixtures in place (`LpAffineExpression` supports `__setitem__` for coefficient mutation; `LpConstraint` does NOT — built a fresh minimal problem for the negative-constraint-coefficient case instead of mutating).
- Verify GREEN: solvers 33→42 pytest, ruff+format clean, pyright 0×solvers (3 fewer suppressions). experiments 80 pytest incl e2e golden (NO regen — GREEDY path now validated but tiny fixture is well-formed: binary vars, nonneg COST_ORDINAL utilities, single PB constraint, empty `constraints_configs`). core 1 pytest, ruff+pyright clean. bindings untouched (not touched this ticket, but solvers venv already has it built — MES-Add1 etc. tests ran, none skipped).

### From T16 (PR feat/t16-dedupe-feasibility, 2026-07-19)

- **Transform deduped**: experiments' dead `experiments/src/helpers/transformers/molpToSimpleElection.py` (0 importers, verified grep) `git mv`→ `experiments/archived_code/molpToSimpleElection.py` (NOT deleted). Its `Election` TypedDict was the OLD approvals-only shape (`voters: dict[str,list[str]]`) — superseded by solvers' `election_solver.py::molp_to_simple_election` (utility-aware `Election`). `archived_code` already pyright-excluded (pyrightconfig.json, pre-existing); ADDED `extend-exclude=["archived_code"]` to experiments `[tool.ruff]` so pre-commit/ruff skip it too.
- **Feasibility unified into ONE impl** = new `election_solver.py::FeasibilityChecker`. `ElectionSolver.is_feasible(lp)` static kept as single-shot wrapper (`FeasibilityChecker(lp).check()`) for back-compat/tests. Replaced `lp.valid()` call sites: Phragmen (`phragmen.py` 2 sites, in `phragmen_cardinal`), MES-Exponential (`mes_exponential.py::equal_shares_exponential`), Greedy (was already `self.is_feasible`, now reuses a checker). Each solver builds ONE checker before its candidate loop.
- **Why not plain `lp.valid()`** (meeting notes, now encoded in checker docstring): with a lower-bound GE constraint a partial assignment can violate a GE bound later selections would satisfy → `valid()` wrongly rejects. So GE-present → LP-solve a completion ILP (accepted cands fixed lowBound=1, rest free binary, obj 0, feasible⇔Optimal). No GE → delegate to `lp.valid()` (pure in-memory, NO CBC). `has_lowerbound_constraint` computed ONCE in `__init__` (avoids per-check `any(sense==GE)` scan that would've regressed Phragmen's O(cands²) valid()-loop).
- **Optimization** = model+solver REUSE. Old `is_feasible` rebuilt a fresh `LpProblem` + `PULP_CBC_CMD` every candidate; `FeasibilityChecker` builds the completion sub-model + solver once, each `check()` only flips `lowBound` on the fixed vars and re-solves. **Speedup ~1.14x** end-to-end on the LP-solve (GE) branch (30-cand instance, 1 LE + 1 GE, greedy-style 30-check sweep x10: OLD 5.17s → NEW 4.53s). Bounded by the CBC **subprocess** which runs identically either way (~15ms/solve dominates; PULP_CBC_CMD has no persistent process, so warm-start can't remove it) — reuse only saves model construction (~0.1-2ms/check). **The common non-GE path (ALL e2e golden instances) is untouched: still pure `lp.valid()`, zero CBC.** Primary wins are dedup + correctness (LB-aware feasibility now everywhere, not just Greedy), speedup is secondary.
- Golden IDENTICAL, NO regen: Phragmen/MES-Exp e2e instances have no GE constraint → checker == old `lp.valid()` exactly. Greedy LB unit tests (`test_greedy_solver_lb_*`, the only GE-branch coverage) unchanged — completion ILP is deterministic (obj 0), reuse gives identical status.
- New `solvers/tests/test_feasibility.py` (3 tests): non-LB delegates to valid(), is_feasible==checker, LB reused-checker == fresh-checker per assignment (locks model-reuse correctness). solvers 51→54.
- Bonus cleanup: `phragmen_cardinal` return annotation `set[str]`→`list[str]` (it always returned a list) — removed the `# pyright: ignore[reportReturnType]` at `phragmen.py:409` (T03/T05 suppression inventory: one fewer, tagged "T16/T27").
- **DEFERRED (optional, not done — kept diff tight, no golden risk):**
  - `validate_pb_constraint` double-walk (T13 leftover): `validate_election_program` + `molp_to_simple_election` each call it → constraint list walked twice per PB solve. Still cheap at fixture sizes; NOT collapsed. Natural next owner T17/T27.
  - `mes/common.py::get_total_budget_constraint` redundant duplicate-PB-constraint check (redundant post-T13 validation at `actualSolve`, but still standalone-called by `test_common.py` + `prepare_mes_parameters`): left as-is, don't break callers. NOT collapsed.
  - MES-family still silently IGNORES GE constraints (wrong-answer risk, T13 dropped-rule): `FeasibilityChecker` makes MES-Exp GE-aware for its feasibility drop-check, but MES selection logic itself doesn't reject/handle GE — same known gap.
- Verify GREEN: solvers 54 pytest, ruff+format clean, pyright 0 (one FEWER suppression). experiments 80 pytest incl `-m e2e` golden 1 (NO regen, `git status tests/` clean), ruff+format clean, pyright 0. AC greps empty (dead `molpToSimpleElection` unimported; `lp.valid()` gone from phragmen/mes_exponential — only remains centralized in `FeasibilityChecker.check`). core/bindings untouched.

### From T18 (PR feat/t18-pyproject-hygiene, 2026-07-19)

- **pytest → dev group**: dropped `pytest==8.4.1` from solvers `[project.dependencies]`; added `pytest (>=8.4,<9.0)` to `[dependency-groups].dev` (mirrors core dev group). Relock → lock `pytest` `groups=["dev"]` (was `["main"]`). solvers lock changed; experiments lock UNAFFECTED (`poetry check --lock` → All set!, path-dep hash stable, T08 precedent held). AC-literal: `poetry sync --only main` → `import pytest` ModuleNotFoundError (restored via `poetry sync`). `poetry show --only main | grep pytest` empty.
- **Wheel METADATA before/after** (`poetry build` solvers):
  - BEFORE: `muoblp (>=1.0.4,<2.0)` / `pulp (==3.3.2)` / `muoblpbindings (>=0.0.17,<0.1)` / `pytest (==8.4.1)` ← runtime, wrong.
  - AFTER: same three, NO pytest, NO `file://`. `poetry check` → All set!.
- **Hybrid PEP-621/poetry style is POLICY, not accident** (ROADMAP-wording override, documented in PR #52): ROADMAP T18 text "single poetry style (drop mixed PEP-621/poetry)" deliberately reinterpreted as KEEPING the hybrid. Static `[project.dependencies]` version pins + `[tool.poetry.dependencies]` path-only enrichment IS the T08 fix (Poetry 2.x: dep in both tables → `[project]` pin in wheel METADATA, `[tool.poetry]` path for dev install/lock). Flattening to pure `[tool.poetry]` would resurrect the `file://`-in-METADATA bug T08 killed. Comment above `[tool.poetry.dependencies]` extended to encode this ("do not move deps back here alone"). Do NOT flatten in future tickets.
- Bindings floor stays `>=0.0.17,<0.1` — T17 (bumps to 0.0.18) NOT merged when this ran; no re-bump here. If T17 lands later the range adjusts there, nothing else in solvers pyproject changes.
- **Still open (cosmetic, T28)**: solvers `[project.urls]` → `jasieksz/multiobjective-lp` but repo lives at `algorithmic-econ/` (unchanged this ticket; carried from P1 judge verdict).
- Thin ticket — T08 had already done the heavy lifting (path-dep→version-pin rewrite, pulp pin, bindings range). Only real defect left was the misplaced pytest. 2 source commits (pytest move; policy comment). Verify GREEN: solvers 75 pytest, ruff+format clean, pyright 0, poetry check All set!; experiments 80 pytest + e2e golden 1 (NO regen); core 1 pytest. bindings untouched (built from source as path dep, tests ran, none skipped).

### From T15 (PR feat/t15-register-solvers, 2026-07-19)

- **Mechanism chosen: monkeypatch-on-import** (append classes to `pulp.apis._all_solvers`), NOT a manual-call-only `register_solvers()`. Rationale: pulp 3.3.2 `getSolver` builds `{cls.name: cls for cls in _all_solvers}` at call time off the module-level list, and `listSolvers` iterates it — appending to that list in place is the whole contract. New `muoblpsolvers/registration.py` (`SOLVERS` list + `register_solvers()`); `__init__.py` calls `register_solvers()` at end of import (added to `__all__`, exported for explicit/re-register use too). Idempotent (guard `if solver not in _all_solvers`), so double-import or manual re-call is a no-op.
- **Bindings-free safe**: registration only APPENDS classes, never instantiates → no `actualSolve` runs → no lazy binding import (T11 contract intact). Verified: `sys.modules['muoblpbindings']=None; import muoblpsolvers` still registers all 10, `listSolvers()` includes all 10, `getSolver('SingleTransferableVote').available()` → False (truthful), Greedy → True.
- **getSolver round-trips T10 optionsDict**: `getSolver(name, **kwargs)` → `getSolverFromDict(solver.toDict())` reconstructs `optionsDict` (verified MethodOfEqualSharesConstrains cost_modification_base/max_iterations). All 10 have distinct `name` class attrs; all instantiate with `msg=False` (required by `listSolvers`, which passes `{"msg": False}`).
- Registered names (pulp `getSolver`/`listSolvers` keys = solver `.name`, NOT the class names): `ExpandingApprovals`, `Greedy`, `MethodOfEqualSharesAdd1`, `MethodOfEqualSharesConstrains`, `MethodOfEqualSharesExponential`, `MethodOfEqualSharesUtility`, `Phragmen`, `SingleTransferableVote`, `SolidCoalitionRefinement`, `SummedObjectives`. Documented in solvers/README.md ("PuLP registration" section).
- New `tests/test_registration.py` (24 tests: covers-all, getSolver×10, listSolvers×10, idempotent, options round-trip, bindings-free reimport). solvers 51→75 pytest.
- Verify GREEN: solvers 75 pytest, ruff+format clean, pyright 0 (no new suppressions). experiments e2e golden 1 passed (NO regen — registration now runs on `import muoblpsolvers` inside experiments too, solver outputs/selection unchanged). core/bindings untouched (solvers-only ticket).
- Downstream heads-up: any future solver added to `muoblpsolvers` must be appended to `registration.py::SOLVERS` (unique `name`) or it won't register. T14 (msg/timeLimit) and T16 don't touch registration.

### From T14 (PR feat/t14-time-verbosity, #53, 2026-07-19)

- Env: fresh worktree ⇒ fresh venvs. Built solvers+experiments+core on py3.13 with the CLT workaround (`export SDKROOT=$(xcrun --show-sdk-path); export CXXFLAGS="-cxx-isystem $SDKROOT/usr/include/c++/v1"`) — bindings compile from source as path dep; no SDKROOT ⇒ fails (broken-CLT). experiments/core pulled bindings 0.0.17 fine. All bindings-backed tests ran (none skipped — bindings present).
- **D8 DECIDED: warn+ignore** for binding-backed solvers (STV, ExpandingApprovals, SolidCoalitionRefinement, MES-Add1, MES-Utility) — `warnings.warn(f"{self.name} does not support timeLimit; solving without limit")` as FIRST stmt of `actualSolve`, then solve unlimited. MES-Constrains honors coarsely per-iteration (deadline checked at top of `while iteration < max_iterations`, aborts between iterations — the C++ `equal_shares_utils` call itself is uninterruptible; no warn).
- **Abort status = `LpStatusNotSolved`** (conservative: if deadline passed when the algorithm returns, status is NotSolved even if it happened to finish). Pure-python deadline solvers: Greedy, Phragmen, MES-Exponential, MES-Constrains. Deadline = `time.monotonic() + self.timeLimit if self.timeLimit is not None else None`. `time.monotonic()` allowed by AC grep (only `time.time()` banned).
- **meta `time` semantic change (downstream, P3)**: `experiments/src/problemRunner.py` meta `time` now = `problem.solutionTime` (pulp start/stopClock around `actualSolve`) — SOLVE-ONLY, no longer load+transform+solve. `RunnerResult.time: float` type unchanged. Invisible to e2e golden (`time` normalized away by `golden_utils.py`), so NO regen; verified `git status experiments/tests/` clean. `import time` dropped from problemRunner.
- **DEVIATION from plan (Task 6)**: added a `for variable in lp.variables(): variable.setInitialValue(0)` pre-loop zeroing in `mes_constrains.py::actualSolve`. Plan's abort test asserts `all(v.value()==0)` but MES-Constrains is a plain `LpSolver` (not `ElectionSolver`, which zeroes vars incl. pulp's lazy `__dummy` at `election_solver.py:39-40` before solving). Without the pre-zero, an abort at iteration 0 leaves `__dummy` unmaterialized → `None` → test fails. Greedy/Phragmen (ElectionSolver subclasses) already get this for free.
- **DEVIATION from plan (Task 3)**: plan's Greedy rewrite used `self.is_feasible(lp)`; base already had T16's `FeasibilityChecker(lp)` + `checker.check()` pattern — preserved that (built ONE checker before the loop, deadline-checked inside), only added deadline/status/msg-gates around it. Same for Phragmen/MES-Exp (checker was already there).
- SummedObjectives is the ONE solver with a REAL enforced timeLimit — now forwards `msg=self.msg, timeLimit=self.timeLimit` to `PULP_CBC_CMD`/`GUROBI_CMD` (CBC/Gurobi enforce natively). No warn.
- `prepare_mes_parameters(lp, msg=True)` gained `msg` gate on its zero-vote-removal log (was a bare `print`); all 4 MES solvers pass `msg=self.msg`. `break_ties` (mes_exponential) gained `msg` param gating its tie warning; fixed "Tie-breakign"→"Tie-breaking" typo.
- Known granularity limits (noted in PR): Phragmen inner `while not select_candidate:` doubling loop NOT deadline-checked (abort granularity = one selection round); MES-Constrains abort is between iterations only.
- Verify GREEN: solvers 75→103 pytest (+28 in new `tests/test_time_verbosity.py`), ruff+format clean, pyright 0 (NO new suppressions). experiments 80 pytest incl e2e golden 1 (NO regen), pyright 0. core 1 pytest. AC greps empty (`time.time()`, `print(` absent from `solvers/src/`).
- test file quirk: pre-commit ruff F401 rejects not-yet-used imports, so `test_time_verbosity.py` imports are added task-by-task as first used (matches plan note).

### From T17 (PR feat/t17-coverage-sweep, 2026-07-19)

- **Coverage sweep**: every solver module now ≥1 solve test asserting status + selection. New test files: `test_phragmen.py`, `test_mes_exponential.py` (`budget_init=1`), `test_mes_constrains.py` (skip-if-unavailable), `test_summed_objectives.py` (CBC path), `test_ordinal_solvers.py` (EA/STV/SCR via new `ordinal_pb` fixture). Renamed `test_mes_base.py`→`test_mes_utility.py` (+ fn `test_base_mes_solver`→`test_mes_utility_solver`) — closes T12 misnomer flag. solvers 103→115 pytest.
- **Frozen-expectation provenance**: all new expected selections are CHARACTERIZATION values, frozen by running each solver on the exact fixtures on this base lineage (bindings 0.0.18, pulp 3.3.2, T14+T16 behavior-preserving). Every one matched at runtime first try (no STOP). Phragmen APPROVAL=[A,D,E,F]/COST=[A,C,E,F]; MES-Exp APPROVAL=[A,C,D,F]/COST=[A,B,D]; MES-Constrains == MES-Utility (APPROVAL=[A,D,E]/COST=[B,D,F], iteration-0 convergence on unconstrained fixture — intentional wiring check); Summed APPROVAL=[A,C,E,F]/COST=[A,B,C]; ordinal EA=[B]/STV=[A,D]/SCR=[A,B]. If any changes later → regression, not stale plan.
- **Bug fix 1 (greedy zero-vote)**: T02 leftover (assigned "T13 or T17"; T13 shipped without it) — struck above. `greedy_solver.py` sort-key + filter now `.get(candidate,0)`; candidate in PB constraint but not in any ballot (`profile`) no longer KeyErrors. Regression test added.
- **Bug fix 2 (C++ `__dummy`)**: `bindings/src/common.cpp::Instance::from_MuoblpProblem` now `continue`s on `name=="__dummy"` (mirrors every python-side validator). pulp's `LpProblem.solve` runs `fixObjective` (injects `__dummy` when no scalar objective) BEFORE `actualSolve`, so EA/STV/SCR were UNUSABLE via `problem.solve()` (`ValueError: candidate __dummy has 0` unit-cost check). Direct `actualSolve()` worked (no fixObjective). Fix also removes the old `py::print` `__dummy` Continuous warning → `test_ordinal_solvers.py` asserts stdout empty (closes T14's deferred msg coverage for these three).
- **bindings 0.0.18**: `bindings/pyproject.toml` 0.0.17→0.0.18; `solvers/pyproject.toml` floor `>=0.0.18,<0.1` + relock (experiments lock UNAFFECTED, path-dep hash stable). Local rebuild via `pip install --force-reinstall --no-deps ../bindings` (CLT workaround). **Tag/PyPI publish DEFERRED** (P1 leftover) — NO `bindings@0.0.18` tag pushed.
- **CI bindings-cache bust expected**: T07 cache keys hash `bindings/src/**` + `bindings/pyproject.toml` → this C++ + version edit busts solvers/experiments caches, PR jobs rebuild bindings from source. First real chance to exercise the source-rebuild path with a changed `Instance` reader.
- Verify GREEN: solvers 115 pytest, ruff check + format clean, pyright 0 (no new suppressions). experiments 80 pytest + `-m e2e` golden 1 (NO regen, `git status tests/` clean — e2e solvers GREEDY/MES_UTILS/MES_ADD1 don't use the `Instance` reader). core 1 pytest.
- Double-walk of `validate_pb_constraint` (T13/T16 deferred) NOT collapsed here — out of scope, still cheap. Carried to T27.

### P2 judge verdict (2026-07-19)

- **P2 VERIFIED COMPLETE @ff5533c — GO for P3.**
- Matrix: core 1 / solvers 115 (FRESH venv, bindings 0.0.18 source-built w/ CLT workaround, 0 skipped) / experiments 80 incl. e2e ×2 deterministic (NO regen, `git status tests/` clean); ruff+format clean ×3; pyright 0 ×3.
- Contract sweep 10/10 (ctor `(msg=False, timeLimit=10)`, toDict roundtrip via `getSolverFromDict`, `getSolver`, `listSolvers`); bindings-independence OK (stubbed import → all 10 importable, PURE available()=True, BACKED=False); AC greps clean: `solver_options`/`time.time()`/`print(` empty in solvers/src; `lp.valid()` exactly 1 code site (`election_solver.py:112` FeasibilityChecker fast path); `molp_to_simple_election` gone from experiments/src (archived `experiments/archived_code/molpToSimpleElection.py` — NOTE plan said `archived_code/experiments/`, actual layout per-project, substance OK).
- Status contract: `return lp.status` ×9 (Summed delegates); `LpStatusOptimal` asserted in all 9 solve-test files. Targeted suites test_time_verbosity + test_validation pass.
- Coverage: all 10 modules covered — greedy→test_greedy, phragmen→test_phragmen, mes_add1/utility/constrains/exponential→test_mes_*, summed→test_summed_objectives, EA/STV/SCR→test_ordinal_solvers; no test_mes_base (renamed).
- Packaging: `poetry check` All set!; wheel METADATA exactly `muoblp (>=1.0.4,<2.0)` + `muoblpbindings (>=0.0.18,<0.1)` + `pulp (==3.3.2)`, NO pytest, NO file://; `poetry sync --only main` → pytest ModuleNotFoundError; restored, 115 green.
- Sample: metrics == frozen T01/T05 refs EXACT (APPROVAL 0.0033/219239/167; COST 0.0035/1.34763e11; COST_ORDINAL 0.0032/2.79444e11; bronowice 0.0666/4.35159e9); cache-hit rerun OK (4× "Result already present", 2.7s vs 38s). CI green @ff5533c: test/pyright ×3 + build-bindings all success — Linux e2e tie-break risk NOT materialized, C++ 0.0.18 cache-bust rebuild exercised.
- Findings: BLOCKING none. NON-BLOCKING: ROADMAP T14/T15/T16 PR-link format fixed this session (T16 had PR #51 mislabeled as GH issue).
- Adjudication (T10–T18 leftovers): T01 empty-solver_options crash → CLOSED (T10 defaults; MES-Exp budget_init → PulpSolverError T13). T02 greedy zero-vote → CLOSED (T17 + regression test). test_mes_base misnomer → CLOSED (T17 rename). D8 → CLOSED (T14 warn+ignore, ROADMAP §6). `mes/common.py` dup-PB-check redundancy + `validate_pb_constraint` double-walk → OPEN, T27. MES-family silently ignores GE constraints → OPEN known gap (T13/T16 notes), no owner ticket — consider GH issue at T28.
- Pyright suppression delta (D12): core 1 inline (T27, unchanged); solvers 3 inline (common.py ×2, election_solver.py ×1, all pulp-3.3.2 Optional-name) + config reportArgumentType — down from 7 inline at T05 peak (T13 −3, T16 −1); experiments 2 inline (test_constraint_creation) + 10 config rules — Phase 3 owns ratchet.
- Carried forward: rc-tag publish rehearsal + `bindings@0.0.18` tag/PyPI (NOT pushed; publish order bindings→core→solvers), old muoblpbindings GH repo archive, solvers `[project.urls]` → T28, D12 remaining suppressions, D14 py3.14 matrix.

### From T19 (PR #55, feat/t19-pydantic-models, 2026-07-20)

- **pydantic pinned EXACT `2.13.4`** (user choice over plan's `^2.12`, matches pulp==3.3.2 style). Lock adds pydantic-core/annotated-types/typing-inspection only.
- **NEW experiments dep: explicit `muoblpbindings = {path="../bindings", develop=false}`** — full experiments relock otherwise FAILS: solvers' static PEP-621 `muoblpbindings>=0.0.18,<0.1` unsatisfiable on PyPI (0.0.18 unpublished, T17 leftover) and Poetry 2.x does NOT propagate `[tool.poetry]` path enrichment transitively. First full experiments relock since T08 surfaced this. Keep even after 0.0.18 publishes (dev wiring, package-mode=false so no METADATA risk).
- Solver enum VERBOSE spellings (user-confirmed): `STV`/`SOLID_COALITION_REFINEMENT`/`EXPANDING_APPROVALS` — land in meta/problem filenames of future runs.
- `extra="forbid"` on ALL models. Invalid/stale meta (pre-T19 dirs, stray keys) → resultCache warn + cache miss → silent re-solve overwrites; analyzer → None row (T24 owns structured errors).
- **Plan sequencing bug (for future plan writers)**: plan had `enhanceFromSolverResult` flip to attribute access in Task 2 but its caller (analyzerRunner) stayed dict until Task 3 → analyzer None rows, e2e KeyError Location-Year. Deferred the flip to Task 3.
- **Plan gap**: constraints from config/tests flow INTO `pabutoolsToMoLp` transforms; plan only deduped aliases there. Fixed: `create_constraints_from_config`/`create_category_constraint`/`create_district_constraint` normalize via `ConstraintConfig.model_validate` (accept dicts from 27 `test_constraint_creation` tests + models from runtime). `budget_ratio` missing without `strategy` now raises ValueError (was KeyError). e2e can't catch this path (empty constraints fixture).
- **Golden metrics.json int→float drift is PRE-EXISTING, NOT regenerated**: pipeline emits `"sum": 48069190.0` (pulp 3.x `value()` returns float) vs golden int `48069190`; verified identical drift on base branch; D11 float-rounding in `golden_utils` masks it in compare. ANY future `UPDATE_GOLDEN=1` run will rewrite metrics.json with `.0` suffixes — expect it, justify separately (T24/T26 natural owners). selected.json regen'd here: exactly `instance_size` 11→10 ×3.
- **pyright ratchet: 10 → 3 disabled rules** (experiments pyrightconfig). All-on baseline 120 errors/11 rules. Remaining disabled: `reportArgumentType` 60 (tests pass str/dict literals into enum/model params — T26; pandas — T23), `reportAttributeAccessIssue` 7 (ndarray interop T23; dead `pabutoolsUtils.by_district` `project.district` T25), `reportCallIssue` 3 (pandas sort_values/concat stubs T23). Re-enabled at 0: reportAssignmentType, reportOperatorIssue, reportOptionalSubscript, reportReturnType, reportGeneralTypeIssues, reportInvalidTypeForm, reportTypedDictNotRequiredAccess. Fixes en route: aggregators' `main` annotated `dict` (AnalyzerResult annotation was a lie — they read raw metrics json; T23 puts models back), `instance.meta or {}` guards (pabutools meta Optional), `sense: int` (pulp consts), casts on always-named objectives (B6 int weights preserved).
- aggregateResults.py contains commented-out V2/V3 `def main` blocks — a batch str.replace matched INSIDE a comment and un-commented it (caught by pyright syntax error, reverted). Careful with replace-all there; T23 archives those blocks.
- Root `.gitignore` now ignores `.claude/worktrees/` (embedded worktree repos were getting staged by `git add -A` from repo root).
- Meta jsons now `model_dump(mode="json", exclude_none=True)` — ConstraintConfig entries omit null `budget_ratio`/`strategy` (matches old NotRequired-omitted shape); `RunnerResult` field order in file differs from old handwritten dict order (harmless, json compare key-based).
- Verify GREEN: experiments 93 pytest (+13 test_models incl. 2 sample-config conformance) + e2e golden 1 (×2 consecutive), solvers 115, core 1, ruff+format clean, pyright 0 ×3. Sample smoke metrics == T01/T05 refs EXACT. Broken-config feed → ValidationError `runner_configs.0.solver_type`.

### From T20 (PR #56, 2026-08-25)

- ROADMAP path refs stale post-T19: entrypoints live at `experiments/src/*.py` (`experimentRunner.py`, `analyzerRunner.py`), not `helpers/runners/`; `resultCache.py` at `helpers/utils/`, not `helpers/runners/`. `problemRunner.py` has no `__main__` — it's a helper module, not an entrypoint.
- **New module `helpers/utils/result_naming.py`**: single source of truth for the runner/analyzer meta+problem filename shape (`result_filename`) and its cache-matcher regex (`result_filename_pattern`, builds off the SAME template with a `PROBLEM_ID_PATTERN` wildcard — builder and matcher can't drift apart anymore). Also `data_source_name` (was duplicated `.split('/')[-1].replace('.pb','')` in both `problemRunner.py` and `resultCache.py`) and `new_problem_id`. All 4 pure, unit-tested (`tests/test_result_naming.py`).
- `problemRunner.py`: dropped the nested `get_file_name` closure; builds `problem_path`/`meta_path` via `Path(results_base_path) / result_filename(...)`, kills the trailing-slash f-string concat (`f"{results_base_path}{file}"`). `write_lp`/`RunnerResult.problem_path` still get `str(path)` — model field stays `str`, not changed to `Path` (out of scope, bigger surgery).
- `resultCache.py`: `os.path`/`os.listdir`/hand-rolled regex → `pathlib` + `result_filename_pattern(...)`. Kept `.match()` (not `.fullmatch()`) — preserves exact prior non-anchored-at-end matching semantics, no behavior change.
- **Mutation killed**: `experimentRunner.py`'s in-place `runner_config.results_base_path = ...` loop replaced by `expand_experiment_config.py::resolve_runner_configs(experiment) -> list[RunnerConfig]` (pure, `model_copy` instead of attribute assignment; `experiment.runner_configs` itself never touched). `main()` now threads a local `runner_configs` list through to `pool.map`.
- **Analyzer pool un-hardcoded**: `AnalyzerConfig` gained `concurrency: int = 3` (default preserves old hardcoded `Pool(processes=3)`; `extra="forbid"` safe since optional — no existing config needs updating). `analyzerRunner.py` also fixed the second trailing-slash-dependent path build (`f"{analyzer_result_path}metrics-{...split('/')[-2]}.json"` → `Path(analyzer_result_path) / f"metrics-{Path(experiment_results_base_path).name}.json"`) — this fragile `.split('/')[-2]` dirname recovery was a second, smaller instance of the antipattern beyond `problemRunner.py`'s, not called out by name in ROADMAP text but same root cause.
- Verified byte-identical: sample smoke (real solve, `rm -rf results` first) metrics EXACT match T01/T05/T19 frozen refs (APPROVAL 0.0033/219239/167; COST 0.0035/1.34763e11/0; COST_ORDINAL 0.0032/2.79444e11/0; bronowice 0.0666/4.35159e9/0); analyzer output filename unchanged (`metrics-sample-experiment.json`). e2e golden: NO regen, `git status tests/` clean.
- Runner-script line counts NOT force-reduced to a literal "~50 lines" — `experimentRunner.py`/`analyzerRunner.py` are already thin orchestration post-extraction (heavy lifting — filename/path construction, pool sizing — now lives in lib functions); treated the ROADMAP bullet as a target, not a hard AC line.
- No file renames (T21's job), no model field type changes (`results_base_path`/`problem_path`/etc. stay `str`), no generic "Pipeline" abstraction added (no-new-features).
- New unit tests: `tests/test_result_naming.py` (6, pure fn coverage). `tests/test_models.py` +6: `is_result_present` hit/miss (previously only `is_metadata_content_matching` was covered), `resolve_runner_configs` (fills-missing + preserves-explicit + no-mutation-of-original assertion), `AnalyzerConfig.concurrency` default+override.
- Verify GREEN: experiments 104 pytest (93→104, +11) incl. `-m e2e` golden 1 (NO regen), ruff check+format clean, pyright 0 (no new suppressions — inventory unchanged from T19). solvers/core/bindings untouched (experiments-only ticket, not re-verified).

### From T21 (PR #57, 2026-08-25)

- **`plans/T21-plan.md` was STALE — do not trust it for future reference.** It assumed a post-T20 `pipeline/` package (`pipeline/cache.py`, `pipeline/problem_steps.py`, `pipeline/analyzer_steps.py`) that never existed; T20 actually kept everything under `helpers/` (`resultCache.py` at `helpers/utils/`, `expand_experiment_config.py` at `helpers/transformers/`, already snake_case pre-T21 — untouched here). Rename map + every import-fixup site was re-derived from the live tree + `grep`, not the stale plan. Confirms the T20 leftover's own warning ("ROADMAP path refs stale post-T19") extended to the T21 plan doc too — plan docs written before a dependency ticket lands should be re-verified against the actual merged tree, not assumed correct.
- 17 files renamed (16 in ROADMAP's literal list + `preflibToMuoblp.py`→`preflib_to_muoblp.py`, needed for the "no camelCase filenames" AC grep; 0 importers so pure rename, no import fixup). `generatePhargmenGreedyDistrictExperiment.py`→`generate_phragmen_greedy_district_experiment.py` fixes the typo in the filename only — content already said "phragmen" since T17.
- 3 relative imports in `pabutools_to_molp.py` converted to absolute (`..runners.model`→`helpers.runners.model`, `.pabutoolsConstants`→`helpers.transformers.pabutools_constants`, `.pabutoolsUtils`→`helpers.transformers.pabutools_utils`) — these were the ONLY relative imports anywhere in `experiments/src`.
- **Caught by full-tree grep, not the plan draft**: 3 real imports in `tests/test_models.py` (lines 117/132/167, `from helpers.utils.resultCache import ...`) were initially mis-triaged as "comment mentions" during planning (a 4th hit on the same grep, line 80, genuinely is a comment) — fixed before verify. Lesson: when grep dumps mixed comment+import hits for one symbol, check each line's syntax before bucketing, don't pattern-match on the first hit.
- Also fixed 3 stale comments referencing pre-rename names beyond the plan's original "one stale comment" allowance (all trivial one-liners, no logic): `test_e2e_golden.py` trailing-slash note (T20 already removed the f-string concat), `analyzer_runner.py` + `aggregate_grouped_results.py` "Example usage" docstring comments.
- Aggregators (`aggregate_results.py`, `aggregate_grouped_results.py`) + `generate_experiment.py` + `generate_phragmen_greedy_district_experiment.py`: filename rename only, zero importers, broken `from src.helpers` imports left as-is (T23 owns the fix per ROADMAP).
- Sample smoke (fresh `rm -rf results/*`, real solve, `~38s`): metrics EXACT match every prior frozen ref (T01/T05/T19/T20) — APPROVAL 0.0033/219239/167; COST 0.0035/1.34763e11/0; COST_ORDINAL 0.0032/2.79444e11/0; bronowice 0.0666/4.35159e9/0. Log lines confirm renamed modules actually running (`"module": "experiment_runner"` / `"analyzer_runner"`).
- Verify GREEN: experiments 104 pytest (unchanged count — pure rename, no new tests) incl. `-m e2e` golden 1 (NO regen, `git status tests/` only shows import-line diffs), ruff check+format clean, pyright 0 (no new suppressions). solvers pytest green, core pytest green (both untouched, confirmed not just assumed). AC greps clean: no camelCase filenames, no "Phargmen", no relative imports, no old-name residue anywhere in `experiments/` (only historical `plans/T02,T14,T19,T20-plan.md` still mention old names — those are frozen records, not live docs, left untouched).
- CI paths: confirmed no-op — `.github/workflows/*.yml` and `pyrightconfig.json` have no experiments module-path references to update.

### From T22 (PR feat/t22-config-generators, 2026-08-30)

- **`plans/T22-plan.md` archive-path claim was wrong** — plan draft said `archived_code/experiments/` (top-level); actual repo convention (established T16) is `experiments/archived_code/`. Corrected during planning (Explore-agent verification), archived both superseded scripts there. Everything else in the plan draft (line numbers, function names, dup-loop correspondence, file lengths) verified exact against the merged post-T21 tree — no other drift.
- Result: 1 interactive entrypoint (`generate_experiment_config.py`, now emits full OR compact via a new `output format` prompt) + 1 sweep entrypoint (`generate_sweep_config.py`, renamed+rewritten from `generate_phragmen_greedy_district_experiment.py`, driven by a JSON `SweepSpec`). `generate_compact_experiment_config.py` + `generate_experiment.py` → `experiments/archived_code/` (git mv, history preserved; zero importers confirmed via grep before moving).
- New pure `build_runner_configs(paths, solvers_with_options, utilities, source_type, experiment_results_base_path, constraints_configs_path, deduplicate_objectives) -> list[RunnerConfig]` in `generate_experiment_config.py` — extracted from the previously-inlined cartesian-product loop; both entrypoints import it, zero dup helpers left (`filter_paths` also single-sourced, `generate_sweep_config.py` no longer has its own copy).
- New `SweepSpec` model (`helpers/runners/model.py`, next to other config models): `mode`, `root_path`, `pattern_groups`, `solvers: list[SolverSpec]`, `utilities: list[Utility] | None = None`, `concurrency`, `experiment_results_base_path`, `constraints_configs_path`, `deduplicate_objectives`, `output_path`. `source_type` deliberately omitted (PABUTOOLS only implemented `Source`, matches old hardcoded sweep script). `utilities` is new *optional* surface — no prior compact/sweep path expressed it (only dead commented-out code in the interactive `__main__`); defaults to `None`, zero behavior change if unused.
- New pure `parse_pattern_groups(raw: str) -> list[list[str]] | None` replaces the hardcoded placeholder list in `generate_experiment_config.py`'s `__main__` (was 2 separate hardcoded copies across interactive + compact scripts, now 1 prompt: `;`-separated groups, `,`-separated patterns).
- Closes the T09 leftover: sweep generator no longer writes into (or reads from) the deleted `resources/…` dir — `output_path`/`experiment_results_base_path` both come from the spec json. Confirmed `grep -rn "resources/" experiments/src --include="*.py"` empty except in the *aggregators* (T23's scope, untouched here) and inside the two now-archived scripts.
- New `experiments/tests/test_generators.py` (13 tests): `SweepSpec` validation, `build_runner_configs` cartesian-size (w/ and w/o utilities) + options passthrough, `generate_from_spec`/`main` roundtrip against new fixture `experiments/tests/fixtures/sweep-spec.json` (points at existing `tests/fixtures/input/krakow_2024_mini`), `parse_pattern_groups` (blank/single/multi/whitespace), import-without-prompting smoke, and one generate→run integration test (`generate_sweep_config.main()` → `experiment_runner.main()` → asserts `meta_*.json` produced, GREEDY-only on `krakow_2024_mini`, no golden involved). Old hardcoded sweep shape (Phragmen ×4 kappa/scaling combos + Greedy) recoverable from git history on the renamed file (`generate_phragmen_greedy_district_experiment.py` pre-rename).
- Manual smoke (non-interactive, since `questionary` needs a real TTY — couldn't drive the actual prompts from this shell): called `generate_experiment_config()` and the compact-branch construction (`CompactExperimentConfig`/`RunnerConfigsGenerator`) directly against `sample-experiment/input`, both validated via `parse_experiment_config`. Interactive prompt flow itself stays untested per ticket's global constraint (`questionary`, not unit-tested by design) — genuine end-to-end interactive click-through not exercised this session.
- Verify GREEN: experiments 117 pytest (104→117, +13 all in `test_generators.py`) incl. `-m e2e` golden 1 (NO regen — generators are outside the runner/analyzer path, `git status experiments/tests/` shows only the new fixture/test file), ruff check+format clean, pyright 0 (no new suppressions). solvers/core/bindings untouched (generators-only ticket, not re-verified).

### From T23 (PR feat/t23-aggregators, 2026-08-30)

- **`plans/T23-plan.md`'s Task-2 archive path was wrong** — it said top-level `archived_code/experiments/`; live convention since T16/T22 is `experiments/archived_code/` (flat). Corrected before archiving. Its "hardcoded Zabrze/Amsterdam filters" claim was actually RIGHT despite ROADMAP's own 2026-08-25 stale-check doubting it (`aggregate_grouped_results.py:82` has them verbatim) — a reminder that stale-checks can themselves be wrong, re-derive from the live file either way.
- **New bug found + fixed (beyond plan), only surfaces on real multi-row data**: `_normalize_relative_to_baseline`'s old-V3-inherited `set_index("City")["Value"]` baseline lookup crashes (`ValueError: truth value of a Series is ambiguous`) whenever the baseline solver has >1 row for the same city — e.g. real `sample-experiment` output has 3 `krakow_2024` rows (one per utility: APPROVAL/COST/COST_ORDINAL). The tiny golden fixture (1 row/city) never hit this, so it shipped invisible until sample smoke. Fixed via `.groupby("City")["Value"].mean()` instead of `.set_index`. No test in `test_aggregator.py` currently exercises the multi-row-per-city path directly (covered manually via sample smoke only) — candidate for T26 coverage sweep if it should be a real unit test.
- **Golden `tests/fixtures/golden/metrics.json` is NOT directly loadable as `AnalyzerResult`** — D11 normalization (`golden_utils.py::normalize_metrics_rows`) strips the `time` field from checked-in goldens. `test_aggregator.py` reinjects a fixed `time` via a local `_rows_with_time(tmp_path)` helper before using it as `load_rows` input. Future tickets reusing this fixture as real pipeline input need the same reinjection.
- **Sample-experiment's canonical `sample-analysis-config.jsonc`** (`metrics: ["EXCLUSION_RATION", "SUM_OBJECTIVES", "EJR_PLUS"]`) never requests `INSTANCE_SIZE`/`TOTAL_COST` — its checked-in `metrics-sample-experiment.json` output has `INSTANCE_SIZE: null` on every row, so bucket-mode aggregation (which buckets by instance size) produces an empty dataframe against that file as-is. Not fixed here (canonical sample config is out of T23's refactor-only scope) — bucket-mode sample smoke instead used a scratch copy of that same file with `INSTANCE_SIZE` injected. T28 (docs) or a future ticket may want to add `INSTANCE_SIZE`/`TOTAL_COST` to the canonical sample analysis config so bucket-mode "just works" out of the box.
- Pyright: attempted removing all 3 disabled rules (`reportArgumentType`/`reportAttributeAccessIssue`/`reportCallIssue`) from `pyrightconfig.json` — aggregator files themselves are now 0-error clean on all 3 (confirmed via `grep`), but repo-wide 73 errors remain elsewhere (63/7/3 split, all in `tests/` — pre-existing per T19/T26 inventory, e.g. `test_pabutools_to_molp.py` enum-literal args). Reverted the config change; ratchet stays T26's job. One NEW pyright error found + fixed in-scope: `_apply_filters`'s `df[bool_mask]` re-assignment triggered `reportAssignmentType` (not one of the 3 disabled rules) — fixed via `.loc[]` instead of `[]`.
- `AggregatorConfig` new model: `metrics_json_path`, `output_path`, `group_by: Literal["instance_size_bucket","city"]`, `bucket_size=10`, `exclude_cities=[]`, `include_solvers: list[str]|None` (matches composed `SOLVER_{options}` label exactly), `normalize_baseline: Solver|None` (bucket mode only, default None per user decision — legacy V3 always normalized, new configs must set it explicitly), `clip_upper=5.0`.
- Verify GREEN: experiments 129 pytest (117→129, +12 all in new `tests/test_aggregator.py`) incl. `-m e2e` golden 1 (NO regen — aggregator sits outside runner/analyzer pipeline, `git status experiments/tests/` shows only the new test file), ruff check+format clean, pyright 0 (config unchanged from T19 baseline). Sample smoke both modes (city mode against real `sample-experiment` output as-is; bucket mode against a scratch copy with `INSTANCE_SIZE` injected) produced real PNGs, visually inspected. AC greps clean: zero commented-out code in `aggregate_results.py`, zero `from src.` imports in `experiments/src`. solvers/core/bindings untouched (experiments-only ticket, not re-verified, matches T20-T22 precedent).
