name: Lint

on: [push]

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: astral-sh/ruff-action@v3
        with:
          src: >-
            experiments/
            core/
            solvers/
      - run: ruff check --fix --exclude archived_code
      - run: ruff format
